# Skills vendored from other repositories. See docs/vendoring.md.
#
# Vendored copies are PRISTINE: never patch them in-tree, send fixes upstream.
# `commit` is the pinned upstream SHA; scripts/vendor-skills.sh bumps it, and
# the vendor-skills workflow opens the PR that carries the bump.
#
# TOML rather than YAML so the parser is python3 stdlib (tomllib) with no
# third-party dependency to install on a runner.

[[skills]]
name = "unslop"
repo = "https://github.com/cursor/plugins"
ref = "main"
path = "pstack/skills/unslop"
license = "pstack/LICENSE"
commit = "6ed0f7a9504f577d7529064103cecce9be7dfc5e"
# Upstream ships it user-only; I want the model to reach for it on its own.
frontmatter = { disable-model-invocation = false }

# The official herdr CLI skill, from the herdr repo itself: the authority on
# how an agent inside a herdr pane drives panes, tabs, and other agents.
[[skills]]
name = "herdr"
repo = "https://github.com/herdrdev/herdr"
ref = "master"
path = "skills/herdr"
license = "LICENSE"
commit = "8ac9542757292f7a8d42a2d532bc6a8a33c7ffce"

# GitHub's skill for its gh-stack CLI extension, from the extension's repo.
[[skills]]
name = "gh-stack"
repo = "https://github.com/github/gh-stack"
ref = "main"
path = "skills/gh-stack"
license = "LICENSE"
commit = "2bd699a544a09cb5c45a013d03416e0894b0454e"
